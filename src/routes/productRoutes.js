import express from 'express';

const router = express.Router();

router.get("/", (req, res) => {
    //get all products
});
router.post("/", (req, res) => {
    //create a new product
});



export default router;